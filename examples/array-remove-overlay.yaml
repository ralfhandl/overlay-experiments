overlay: 1.0.0
info:
  title: Remove an array element
  version: 1.0.0
actions:
  - target: $.paths.*.get.parameters[?(@.name == 'dummy')]
    remove: true
  - target: $.paths.*.get.tags[?(@ == 'bad')]
    remove: true
  - target: $.paths.*.get.tags[?(@ == 'ugly')]
    update: ok
