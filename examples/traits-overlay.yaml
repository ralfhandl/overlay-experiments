overlay: 1.0.0
info:
  title: Apply Traits
  version: 1.0.0
actions:
  - target: $.paths[?@.get['x-oai-traits'][?@ == 'paged']].get
    update:
      parameters:
        - name: top
          in: query
          # ...
        - name: skip
          in: query
          # ...
