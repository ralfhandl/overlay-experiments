overlay: 1.0.0
info:
  title: insert a primitive array element and update another
  version: 1.0.0
actions:
  - target: $.paths['/items'].get.tags[?(@ == 'beta')]
    update: released
  - target: $.paths['/items'].get.tags
    update: list
